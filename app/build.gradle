plugins {
    id 'com.android.application'
}

android {
    namespace 'in.sursandconnect.app'
    compileSdk 35

    defaultConfig {
        applicationId 'in.sursandconnect.app'
        minSdk 24
        targetSdk 35
        versionCode 11
        versionName '1.1.0'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}
